spring:
  application:
    name: gateway-server
  cloud:
    config:
      #      Для запуска в Докере
      uri: http://config:8888

      #      Для запуска локально
      #      uri: http://localhost:8888
      fail-fast: true